import { createSlice } from "@reduxjs/toolkit";

const movieDetailSlice = createSlice({
  name: movieDetail,
  initialize: [],
  reducers: { fetchMovie() {} },
});

const [] = movieDetailSlice.actions;
export default movieDetailSlice.reducers;
